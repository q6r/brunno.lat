# Last.fm — get a free API key at https://www.last.fm/api/account/create
LASTFM_API_KEY=your_lastfm_api_key_here
LASTFM_USER=crynew

# Discord (public id, consumed via the cee.bio API)
DISCORD_USER_ID=73598582153805824

# RapidAPI key for the Instagram profile endpoint (instagram120). Optional —
# lib/instagram.ts falls back to a built-in key if this is unset.
RAPIDAPI_KEY=your_rapidapi_key_here
